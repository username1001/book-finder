# Book Finder

A React project using the google books API.

## Development

`fork` this repo
<br>
`git clone https://github.com/<your_username>/book-finder`
<br>
`cd /book-finder`
<br>
`npm install`
<br>
`npm start`
